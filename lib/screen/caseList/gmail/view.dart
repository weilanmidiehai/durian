import 'package:flutter/material.dart';
import 'package:get/get.dart';

import 'logic.dart';

class GmailPage extends StatelessWidget {
  GmailPage({Key? key}) : super(key: key);

  final logic = Get.put(GmailLogic());

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
